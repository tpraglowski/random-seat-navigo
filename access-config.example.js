// Local-only file (gitignored) — real value for local testing.
// The deployed site does NOT use this file; GitHub Actions generates it at build time
// from the ACCESS_CODE repository secret (see .github/workflows/deploy-pages.yml).
export const ACCESS_CODE = 'REPLACE_ME';
